# Rewrite rule (Cloudflare Pages / Netlify): /current always mirrors the newest issue.
# Update the destination path below on the 1st of every month, then commit + push.
# Status 200 = rewrite (URL bar keeps showing /current), not a redirect.

/current       /2026-09/   200
/current/      /2026-09/   200
